[project]
name = "automobile-analysis"
version = "0.1.0"
description = "Automobile data analysis and dashboard"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
